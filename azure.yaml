# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: zava-storefront
metadata:
  template: zava-storefront
infra:
  provider: bicep
  path: ./infra
  module: main
services:
  web:
    project: ./src
    host: appservice
    language: dotnet
    docker:
      path: ./Dockerfile
      context: .
